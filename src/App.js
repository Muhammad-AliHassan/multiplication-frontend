// Import the Multiplier component from the current directory
import Multiplier from "./Multiplier";

// Main App component
function App() {
  return (
    <div className="App">
      <Multiplier/>
    </div>
  );
}

// Export the App component so it can be used in other parts of the application
export default App;
